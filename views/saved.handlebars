<nav class="navbar navbar-default">
    <div class="container-fluid">
        {{!--
        <!-- Brand and toggle get grouped for better mobile display -->--}}
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1"
                aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">Mongo News Scraper</a>
        </div>
        {{!--
        <!-- Collect the nav links, forms, and other content for toggling -->--}}
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                <li>
                    <a href="/" class="navButton">Home</a>
                </li>
                <li>
                    <a href="/saved" class="navButton">Saved Articles</a>
                </li>
            </ul>
            <form class="navbar-form navbar-left" method="GET" action="/scrape">
                <button type="submit" class="btn btn-default">Scrape</button>
            </form>
        </div>{{!--
        <!-- /.navbar-collapse -->--}}
    </div>{{!--
    <!-- /.container-fluid -->--}}
</nav>
{{!-- jumbotron --}}
<div class="jumbotron">
    <h1>Saved Articles</h1>
    <p>Saved articles from the Smithsonian Magazine Website</p>
    <p>All articles are the property of the Smithsonian Institution</p>
</div>

<!--Saved Articles Page-->

<div class="outer-container">

    <div class="jumbotron jumbo-nyt">
        <div class="container">
            <div class="text-center">
                <h1> Saved Articles</h1>
            </div>
        </div>
    </div>
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                {{#if article}} {{#each article}}
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <h4>{{this.title}}</h4>
                    </div>
                    <div class="panel-body">
                        <p> {{this.summary}}</p>
                        <a href={{this.link}} target="_blank">{{this.link}}</a>
                        <br>
                        <br>
                        <button type="button" class="btn btn-success addNote" data-toggle="modal" data-target="#noteModal{{this._id}}" data-id={{this._id}}>Add a Note</button>
                        <button type="button" class="btn btn-danger delete" data-id={{this._id}}>Delete</button>
                    </div>
                </div>
                {{/each}} {{else}}
                <div class="panel panel-default">
                    <div class="panel-body">
                        <h4>You have no saved articles.</h4>
                    </div>
                </div>
                {{/if}}
            </div>
        </div>
    </div>
</div>

<!--Clicked Notes Modal-->
{{#each article}} {{> notes-modal }} {{/each}}